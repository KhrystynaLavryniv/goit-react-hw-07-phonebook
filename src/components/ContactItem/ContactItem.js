// import { useGetContactsQuery, useDeleteContactsMutation } from 'redux/contactsSlice';

// export const ContactItem = ({ id, contact }) => {

//   const {
//     data: contacts,
//   } = useGetContactsQuery();

//     const [deleteContacts, { isLoading: isDeleting }] =
//     useDeleteContactsMutation();
//   return (
//     <li>
//       {contact}
//       <button onClick={() => deleteContacts(id)}>
//         {isDeleting ? 'Deleting...' : 'Delete'}
//       </button>
//     </li>
//   );
// };
